{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "Exercises for Part 3 of fullstackopen.com",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../phonebook-frontend/ && npm run build && cp -r dist ../phonebook-backend",
    "commit:deploy": "git add . && git commit -m",
    "deploy:all": "npm run build:ui && npm run commit:deploy"
  },
  "author": "sanli",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}